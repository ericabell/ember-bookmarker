<h3>Users</h3>
<p>{{testProperty}}</p>
{{#each model as |user index|}}
  <p>#{{index}} - {{user.fullName}}</p>

  {{#each user.bookmarks as |bookmark|}}
    <p>{{bookmark.title}}</p>
  {{else}}
    <p>No Bookmarks for this user!</p>
  {{/each}}
{{/each}}
{{outlet}}
